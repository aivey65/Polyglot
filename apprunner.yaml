version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  runtime-version: 3.8.16
  command: sh startup.sh
  network:
    port: 8000
entrypoint: gunicorn -c gunicorn.conf.py -b :8080 PolyglotProject.wsgi:app